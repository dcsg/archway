{{- /* AI context file — generated by Archway */ -}}
# Soul — {{.ServiceName}}

## What This Is

{{.ServiceName}} is a Go service built with hexagonal architecture (ports & adapters).
{{- if .HasHTTP}} It exposes HTTP endpoints via Chi router.{{end}}
{{- if .HasGRPC}} It exposes gRPC services.{{end}}
{{- if .HasKafka}} It processes messages from Kafka.{{end}}

## Stack

- **Language:** Go {{.GoVersion}}
- **Architecture:** Hexagonal (ports & adapters)
{{- if .HasHTTP}}
- **HTTP Router:** Chi
{{- end}}
{{- if .HasGRPC}}
- **RPC:** gRPC + buf
{{- end}}
{{- if .HasKafka}}
- **Messaging:** Kafka
{{- end}}
{{- if .HasPostgreSQL}}
- **Database:** PostgreSQL
{{- end}}
{{- if .HasMySQL}}
- **Database:** MySQL
{{- end}}
{{- if .HasRedis}}
- **Cache:** Redis
{{- end}}
{{- if .HasFeatureFlags}}
- **Feature Flags:** OpenFeature pattern (memory + env providers)
{{- end}}
- **Observability:** OpenTelemetry, structured logging (slog)
- **Lifecycle:** Graceful shutdown, health checks

## Critical Rules

1. Domain logic MUST NOT import infrastructure packages
2. All external boundaries MUST be defined as interfaces in `port/`
3. Adapters MUST NOT contain business logic
4. All errors MUST be domain-specific types or wrapped with context
5. All public functions MUST have tests
{{- if .HasGolangCILint}}
6. All code MUST pass golangci-lint before merging
{{- end}}

---

*Generated by [Archway](https://github.com/dcsg/archway) on {{date "2006-01-02" now}}*
