{{- /* AI context file — generated by Archway */ -}}
# Soul — {{.ServiceName}}

## What This Is

{{.ServiceName}} is a Go CLI application with a minimal, flat structure focused on simplicity and ease of use.

## Stack

- **Language:** Go
- **Architecture:** Flat (single-purpose CLI)
- **Dependencies:** Prefer stdlib

## Critical Rules

1. Keep the dependency tree minimal — prefer stdlib
2. All public functions MUST have tests
3. Error messages MUST be user-friendly and actionable
4. Use structured exit codes (0 = success, 1 = error)
{{- if .HasGolangCILint}}
5. All code MUST pass golangci-lint before merging
{{- end}}

---

*Generated by [Archway](https://github.com/dcsg/archway) on {{date "2006-01-02" now}}*
