---
title: Archway
description: Compose production-grade services from architecture patterns and 38 capabilities
template: splash
hero:
  tagline: Architecture-aware service composer for Go
  actions:
    - text: Get Started
      link: /archway/getting-started/quickstart/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/dcsg/archway
      variant: minimal
      icon: github
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components';

Stop writing boilerplate. Compose production-grade services from architecture patterns and **38 modular capabilities**. Pick your structure, pick what your service needs — Archway wires it all together.

You own the code. No runtime dependency. No lock-in.

```
Architecture (hexagonal) + Capabilities (http-api, mysql, docker) = Your Service
```

<CardGrid>
  <Card title="38 Capabilities" icon="puzzle">
    Transport, data stores, security, resilience, observability, i18n, feature flags, CQRS, and more. Each is self-contained and composable.
  </Card>
  <Card title="Smart Wizard" icon="star">
    The interactive wizard suggests what you're missing. Adding HTTP? You probably want rate limiting, CORS, and auth too.
  </Card>
  <Card title="Architecture Enforcement" icon="approve-check-circle">
    `archway check` validates dependency rules, detects anti-patterns, and measures compliance — with 11 AST-based detectors.
  </Card>
  <Card title="Zero Lock-in" icon="open-book">
    Generates plain Go code with no Archway runtime. Scaffold once, own the code forever.
  </Card>
</CardGrid>

## See It in Action

```bash
# Interactive wizard — guides you through every choice
archway new my-service

# Or go non-interactive
archway new my-service \
  --arch hexagonal \
  --cap platform,bootstrap,http-api,mysql,docker \
  --module github.com/myorg/my-service \
  --no-wizard
```

You get a production-ready service with:
- Hexagonal architecture with strict dependency rules
- REST API with Chi router and RFC 7807 errors
- MySQL with connection pooling and health checks
- Structured logging, OpenTelemetry, PII redaction
- Bootstrap pattern for testable dependency wiring
- Docker Compose for local development
- `archway.yaml` for architecture enforcement

<Aside type="tip">
Run `archway check` at any time to validate your project follows its own architecture rules. It catches dependency violations, missing directories, and function complexity issues.
</Aside>

## Capabilities at a Glance

| Category | Capabilities |
|----------|-------------|
| **Transport** | HTTP API, gRPC, Kafka consumer, WebSocket |
| **Data** | MySQL, PostgreSQL, Redis, migrations |
| **Security** | JWT auth, rate limiting, CORS |
| **Resilience** | Circuit breaker, retry, idempotency, outbox |
| **Patterns** | CQRS, event bus, repository, worker, scheduler |
| **Observability** | Health checks, OpenTelemetry, request ID, audit log |
| **Infrastructure** | Docker, CI/GitHub, validation, UUID, feature flags |
| **Cross-cutting** | i18n, email gateway, Mailpit, API versioning |

<CardGrid>
  <Card title="Read the docs" icon="right-arrow">
    [How It Works](/archway/concepts/how-it-works/) — understand the composition model
  </Card>
  <Card title="Try it now" icon="rocket">
    [Quick Start](/archway/getting-started/quickstart/) — create your first service in 2 minutes
  </Card>
</CardGrid>
