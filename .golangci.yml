version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosimple
    - gocritic
    - misspell
    - gofmt
    - goimports
    - bodyclose
    - noctx
    - prealloc
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gocritic
      - text: "strings.Title is deprecated"
        linters:
          - staticcheck
